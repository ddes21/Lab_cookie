class Config:
    # Example secret (replace in real apps)
    SECRET_KEY = "dev-secret-key"

    # Hardcoded credentials (kept same as original)
    USERS = {
        "user": "password123",
        "admin": "supersecret123456"
    }
